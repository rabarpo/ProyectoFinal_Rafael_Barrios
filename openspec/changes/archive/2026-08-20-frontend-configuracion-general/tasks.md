# Tasks: Configuración general (Backlog #28)

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~1,295 total across the chain (see per-PR table below) |
| 400-line budget risk | Medium if delivered as the design's suggested 4 PRs (PR3 "Datos institucionales" bundles `CampoDominios` + `PanelDatosInstitucionales` + wiring at design.md's own ~430-line estimate — already over the 400-line budget at forecast time, not just at risk of creeping over); Low with the 5-PR split below |
| Chained PRs recommended | Yes |
| Suggested split | 5 PRs (`feature-branch-chain` on the long-lived branch, tagged commits — no separate git branches, per repo convention) |
| Delivery strategy | ask-on-risk |
| Chain strategy | confirmed: 5 PRs, sequential, each leaves the app in a usable/buildable state |

Decision needed before apply: Yes — design.md already flags its suggested PR3 ("Datos institucionales",
~430 lines) as "el corte riesgoso" and pre-documents an exact fallback split into 3a (`CampoDominios`
isolated, ~160) and 3b (`PanelDatosInstitucionales`, ~270) "if the forecast exceeds the 400-line
budget." Unlike `#27`'s PR1 (where the design hadn't sized the bundle at all and the risk was
speculative), here design.md's own estimate is already **above** 400 before any RED/GREEN diff is
counted — the same criterion `#26`/`#27` used ("split proactively when the forecast already exceeds
budget, don't leave it as contingency") applies more strongly here, since the split is fully
pre-specified and the piece (`CampoDominios`) is explicitly described as "autónoma y testeable sola."
Confirmed adjustment: **5 PRs instead of 4**, splitting the design's PR3 into PR3a (`CampoDominios`)
and PR3b (`PanelDatosInstitucionales` + wiring); PR1, PR2, and PR4 keep the design's grouping
unchanged, PR4 renumbered from the design's "PR4" position (unaffected, stays last).

| PR | Contents | Est. lines | Budget risk |
|----|----------|-----------:|-------------|
| PR1 | Cimientos y ruta (D1/D2/D10): `rutas.ts`/`.spec.ts`, `Enrutador.tsx`/`.spec.tsx`, `menu-por-rol.ts`/`.spec.ts` — `Ruta 'configuracion'`, `case`, `CONFIGURACION` → navegable; `ConfiguracionPage.tsx`/`.spec.tsx` with the D10 gate and empty state, **zero fetch** | ~150 | Low |
| PR2 | Cliente API y mensajes-error (D3/D7): `configuracion-api.ts`/`.spec.ts` — `ResultadoApi`, 4 funciones + `urlLogo`; `mensajes-error.ts`/`.spec.ts` — `Record` total sobre los 6 códigos | ~355 | Low-Medium |
| PR3a | `CampoDominios` aislado (D4): `piezas/CampoDominios.tsx`/`.spec.tsx` — alta/baja de dominios, arreglo vacío explícito, guarda mínima local de duplicados/`trim()`. Componente autónomo, sin wiring al contenedor todavía | ~160 | Low |
| PR3b | `PanelDatosInstitucionales` (D5/D6): `paneles/PanelDatosInstitucionales.tsx`/`.spec.tsx` — `FormularioGenerico` para 8 campos string, diff de merge parcial, coerción `smtp_puerto`, wiring de `CampoDominios`; cableado en `ConfiguracionPage` (D6: gate de carga, `key={version}` remount) | ~270 | Low-Medium |
| PR4 | Logo y comité (D8/D9): `validar-logo.ts`/`.spec.ts` — helper puro; `paneles/PanelLogo.tsx`/`.spec.tsx` — `CampoArchivo` + subida + preview con cache-bust; `paneles/PanelComite.tsx`/`.spec.tsx` — `TablaGenerica` sin acciones; wiring final en `ConfiguracionPage` | ~360 | Low-Medium |

Decision needed because design.md flagged its own PR3 estimate as already over the 400-line budget
without applying its own documented fallback. Confirmed adjustment: **5 PRs instead of 4**, splitting
PR3 into PR3a/PR3b exactly as design.md's contingency describes; PR1/PR2/PR4 unchanged. If PR2's
actual diff creeps past ~400 during apply, split `configuracion-api.ts` (D3, the two reads) from
`mensajes-error.ts` (D7, self-contained) into PR2a/PR2b before continuing. If PR4's actual diff creeps
past ~400, split `PanelLogo`+`validar-logo.ts` (D8) from `PanelComite.tsx` (D9, self-contained,
smallest surface) into PR4a/PR4b before continuing.

## Phase 1 (PR1): D1 — `Ruta 'configuracion'` en `rutas.ts`

- [x] 1.1 RED in `apps/frontend/src/app/rutas.spec.ts`: assert `parsearRuta('/configuracion') → { nombre: 'configuracion' }`, `rutaAPath({ nombre: 'configuracion' }) === '/configuracion'`, round-trip `parsearRuta(rutaAPath({ nombre: 'configuracion' }))` deep-equals the literal (spec: `minimal-frontend-router`, "Variante `Ruta 'configuracion'` plana sin sub-rutas").
- [x] 1.2 RED (same file): assert `parsearRuta('/configuracion/logo')`, `parsearRuta('/configuracion/comite')`, `parsearRuta('/configuracion/x')`, and `parsearRuta('/configuracion/../../etc/passwd')` all `→ { nombre: 'no-encontrada', pathname }` (spec: same requirement — "esta ruta MUST NOT requerir sub-rutas ni estado de navegación interna por pestañas"; design.md threat matrix "Enrutamiento (cliente)").
- [x] 1.3 GREEN: in `apps/frontend/src/app/rutas.ts` add `{ nombre: 'configuracion' }` to the `Ruta` union, its `partes.length === 1 && partes[0] === 'configuracion'` branch in `parsearRuta` (placed alongside the `academica`/`usuarios`/`cuentas-bloqueadas` flat-route block), and the matching `case` in `rutaAPath`. Update the file's top comment to mention the new variant (D1: singleton, no sub-routes, no id embedded).

## Phase 2 (PR1): D1 — `Enrutador.tsx` case

- [x] 2.1 RED in `apps/frontend/src/app/Enrutador.spec.tsx`: `/configuracion` mounts `ConfiguracionPage` (test marker, same pattern as `/academica`/`/usuarios`).
- [x] 2.2 GREEN: in `apps/frontend/src/app/Enrutador.tsx` add `import { ConfiguracionPage } from '../configuracion/ConfiguracionPage';` and `case 'configuracion': return <ConfiguracionPage />;`. Update `Enrutador.tsx`'s top comment (wiring history) to note `configuracion`'s origin (`#28`).

## Phase 3 (PR1): D2 — `menu-por-rol.ts`

- [x] 3.1 RED in `apps/frontend/src/app/menu-por-rol.spec.ts`: update the `administrador`/`director` item-set assertions so `CONFIGURACION` is `{ clase: 'navegable', id: 'configuracion', ruta: { nombre: 'configuracion' } }` instead of the current `proximamente` placeholder. Confirm this fails against the current constant.
- [x] 3.2 RED (same file): assert `comite`'s item set does **not** include `configuracion` (spec: `configuracion-institucional`, "Comité no ve el item de menú `configuracion`"); assert `docente`/`estudiante` don't include it either (both already resolve to `[]`, but assert the invariant explicitly since it's part of D2's fail-closed reasoning).
- [x] 3.3 RED (same file): confirm the existing round-trip invariant (every `navegable` item's `ruta` satisfies `parsearRuta(rutaAPath(item.ruta))` deep-equals `item.ruta`) iterates the updated array and covers `configuracion` without a bespoke test.
- [x] 3.4 GREEN: in `apps/frontend/src/app/menu-por-rol.ts` change `CONFIGURACION` from `{ clase: 'proximamente', id: 'configuracion', etiqueta: 'Configuración' }` to `{ clase: 'navegable', id: 'configuracion', etiqueta: 'Configuración', ruta: { nombre: 'configuracion' } }`. **Zero row changes** — `CONFIGURACION` already appears only in `administrador`/`director`'s arrays (D2: `ConfiguracionController` is `@Roles('administrador','director')` at class level, `comite` has no reachable read state). Update the doc comment above `MENU_POR_ROL`.

## Phase 4 (PR1): D10 — `ConfiguracionPage` gate stub, sin fetch

- [x] 4.1 RED in `apps/frontend/src/configuracion/ConfiguracionPage.spec.tsx` (new, `proveer()` pattern with `SesionContext` from `Enrutador.spec.tsx`): role `administrador` or `director` renders the page shell (test marker) without throwing; **zero `fetch` calls** at this stage (no `configuracion-api` mock wired yet — assert via `vi.stubGlobal('fetch', vi.fn())` and expect it uncalled).
- [x] 4.2 RED (same file): role `comite`, `docente`, `estudiante`, or no session (`rol` undefined) renders a `role="status"` message ("Esta sección no está disponible para tu rol" or equivalent) and **zero `fetch` calls** — this is the allowlist gate itself, not a defense-in-depth afterthought (spec: `configuracion-institucional`, "Comité navegando directamente a `/configuracion` no ve la página"; design.md D10: single binary allowlist gate, `puedeGestionar = rol === 'administrador' || rol === 'director'`, written as an allowlist, not a `rol !== 'comite'` denylist — unlike `#27`'s two independent gates, this domain has exactly one because all three sections share the same role set).
- [x] 4.3 GREEN: create `apps/frontend/src/configuracion/ConfiguracionPage.tsx` — `useSesion()` ⇒ `rol`; `const puedeGestionar = rol === 'administrador' || rol === 'director'`; if `!puedeGestionar` render the gate message and return before any fetch; if `puedeGestionar`, render a placeholder shell only (real GET/paneles land in PR2/PR3b/PR4 — this is the throwaway-free real file, no separate stub swap needed since D10's gate is the entire content of PR1's slice).
- [x] 4.4 Full regression for PR1: run `pnpm --filter @seei/frontend test rutas.spec.ts Enrutador.spec.tsx menu-por-rol.spec.ts ConfiguracionPage.spec.tsx` green, then `pnpm --filter @seei/frontend test` (full suite) and `pnpm typecheck` clean before opening PR1. Update `NavegacionPrincipal.spec.tsx` if it still references `configuracion` as its "próximamente" example (same regression class `#27`'s Phase 3.6 hit for `usuarios`) — swap to whichever placeholder item remains (`importacion-excel`) and add a navegable-click assertion for `configuracion` if useful, not itself a task in this list but a direct regression consequence of D2 within PR1 scope.

## Phase 5 (PR2): D3 — `configuracion-api.ts`: `ResultadoApi` + `obtenerConfiguracion`/`actualizarConfiguracion`

- [x] 5.1 RED in `apps/frontend/src/configuracion/configuracion-api.spec.ts` (new, `fetch` mocked the same way `candidatos-api.spec.ts` mocks its writes): `obtenerConfiguracion()` calls `GET /configuracion` and returns `{ ok: true, data }` on a 2xx mock response; `{ ok: false, status }` on a non-2xx mock response (design.md D3: this read carries the envelope too, unlike `#26`'s D6 raw reads — a failed `GET` must not let the container mount the form with garbage values).
- [x] 5.2 RED (same file): `actualizarConfiguracion(input)` calls `PUT /configuracion` with the partial body passed in, `as never` on `body` (contract declares `requestBody?: never`, verified in design.md D3); returns `{ ok: true, data }` on a 2xx mock response.
- [x] 5.3 RED (same file): `actualizarConfiguracion({ color_primario: 'no-es-un-color' })` returns `{ ok: false, status, codigo: 'CAMPO_INVALIDO', campo: 'color_primario' }` on a 400 mock response body `{ codigo, campo }` (design.md D7 precedent: 400s from the three validators carry `{ codigo, campo, motivo }`).
- [x] 5.4 GREEN: create `apps/frontend/src/configuracion/configuracion-api.ts` — copy `ResultadoApi<T>`/`resolver`/`extraerCodigo` from `candidatos-api.ts`, adapted to also extract `campo` (same shape as `usuarios-api.ts`'s `extraerCampo`, per design.md D3's interface); add `ActualizarConfiguracionInput` hand-mirrored from `actualizar-configuracion.dto.ts` per the exact literal in design.md's "Interfaces / Contratos" section (`smtp_puerto?: number`, not `string` — D5, Prisma `Int?` with no `ValidationPipe`); add `obtenerConfiguracion(signal?)` and `actualizarConfiguracion(input)` with `as never` on `body`.

## Phase 6 (PR2): D3 — `configuracion-api.ts`: `listarComite` + `subirLogo` + `urlLogo`

- [x] 6.1 RED (same spec file): `listarComite()` calls `GET /configuracion/comite`; returns `{ ok: true, data: [] }` on a 2xx mock response with an empty array (design.md D3: distinguishes "empty list" from "failed", same reasoning as `#27`'s `listarApoderados`), `{ ok: false, status }` on a non-2xx mock response.
- [x] 6.2 RED (same file): `subirLogo(archivo)` calls `POST /configuracion/logo` with a `FormData` containing key `'logo'` (assert via inspecting the mocked `fetch` call's `body` — mirror `candidatos-api.spec.ts`'s `subirPlanTrabajo` assertion pattern) and **no manual `Content-Type` header**; returns `{ ok: true, data }` on a 2xx mock response with `LogoRespuestaDto`; `{ ok: false, status, codigo: 'LOGO_TAMANIO_EXCEDIDO' }` on a 4xx mock response body `{ codigo }` (design.md D7: the four logo-related 400s are bare `{ codigo }`, no `campo`/`motivo`).
- [x] 6.3 RED (same file): `urlLogo(version)` returns a URL string containing `/configuracion/logo` and a version query param for cache-busting; `urlLogo(undefined)` still returns a valid URL string without throwing (design.md D8: mirrors `urlFoto` from `#12`).
- [x] 6.4 GREEN: add `LogoRespuestaDto`/`UsuarioRespuestaDto` type aliases from `components['schemas']`; add a local `aFormData` helper (replicated from `candidatos-api.ts`'s pattern, per design.md D3's comment "`aFormData` replicado local"); add `listarComite(signal?)`, `subirLogo(archivo: File)`, and `urlLogo(version?: string): string`.
- [x] 6.5 Full regression for PR2 client, part 1: run `pnpm --filter @seei/frontend test configuracion-api.spec.ts` green before moving to Phase 7.

## Phase 7 (PR2): D7 — `configuracion/mensajes-error.ts`

- [x] 7.1 RED in `apps/frontend/src/configuracion/mensajes-error.spec.ts` (new): for each of the 6 `CodigoConfiguracion` values (`CAMPO_INVALIDO`, `LOGO_FORMATO_NO_PERMITIDO`, `LOGO_TAMANIO_EXCEDIDO`, `LOGO_VACIO`, `LOGO_REQUERIDO`, `LOGO_NO_ENCONTRADO`) assert `mensajeDeError({ codigo })` returns a non-generic, non-empty string. Verify the exact literal set against `apps/backend/src/configuracion/configuracion.errors.ts` before writing the union — do not trust design.md's list without re-checking the source file (same discipline `#27`'s Phase 4.1 applied).
- [x] 7.2 RED (same file): assert `mensajeDeError({ codigo: 'CAMPO_INVALIDO', campo: 'color_primario' })` interpolates `'color_primario'`; assert the same for `campo: 'zona_horaria'` and `campo: 'dominios_google'` (spec: `configuracion-institucional`, "Error 4xx del backend se muestra legible sin perder los datos ingresados"; "Dominio con formato inválido se rechaza antes o después del envío" — "la UI muestra el error identificando `dominios_google` como campo inválido"; design.md D7).
- [x] 7.3 RED (same file): assert `mensajeDeError({ codigo: 'CAMPO_INVALIDO' })` (no `campo`) still returns a non-empty string (graceful without interpolation).
- [x] 7.4 RED (same file): assert the status-based fallback when `codigo` is absent — `mensajeDeError({ status: 403 })` and `mensajeDeError({ status: 404 })` return non-generic strings, `mensajeDeError({})` returns a generic non-empty fallback, never `undefined`.
- [x] 7.5 GREEN: create `apps/frontend/src/configuracion/mensajes-error.ts` with `export type CodigoConfiguracion = ...` (6 literals) and `export function mensajeDeError(e: { codigo?: CodigoConfiguracion; campo?: string; status?: number }): string` backed by a `Record<CodigoConfiguracion, string>` total map (compile error, not silent degradation, if a code is added to the backend without updating this map — same discipline as `usuarios/mensajes-error.ts` and `academico/mensajes-error.ts`) plus the status-based fallback chain.
- [x] 7.6 Full regression for PR2 (final phase of PR2): run `pnpm --filter @seei/frontend test configuracion-api.spec.ts mensajes-error.spec.ts` green, then full suite + `pnpm typecheck` clean before opening PR2.

## Phase 8 (PR3a): D4 — `CampoDominios` aislado

- [x] 8.1 RED in `apps/frontend/src/configuracion/piezas/CampoDominios.spec.tsx` (new, mounted standalone with plain props — no `SesionContext`, no API mock, per design.md D4 "presentacional puro y controlado"): renders the current `valor: string[]` as a list; typing a valid domain and confirming the add action calls `onCambiar` with the array plus the new domain appended (spec: `configuracion-institucional`, "Agregar un dominio válido").
- [x] 8.2 RED (same file): with `valor = ['colegio.edu.pe']`, clicking "Quitar" on that entry calls `onCambiar([])` — an explicit empty array, not `undefined` or a no-op (spec: "Quitar el último dominio y guardar arreglo vacío explícito" — this is the piece-level half of that scenario; the `PUT`-time half lands in Phase 11).
- [x] 8.3 RED (same file): adding a duplicate (case-insensitive, e.g. `valor = ['Colegio.edu.pe']` and the user types `colegio.edu.pe`) does **not** call `onCambiar` a second time with a duplicate entry (design.md D4: "guarda mínima local: ignora vacío tras `trim()` y duplicados (`toLowerCase()`)", mirroring the backend's `normalizarYValidarDominiosGoogle` without re-validating format — format validation stays server-side, D7 translates the 400).
- [x] 8.4 RED (same file): adding a blank/whitespace-only string does not call `onCambiar`.
- [x] 8.5 RED (same file): `deshabilitado === true` disables both the add input/button and every "Quitar" action (needed for D6's loading-gate and D10 propagation once wired in PR3b).
- [x] 8.6 RED (same file): the "Agregar"/"Quitar" buttons are `type="button"` and the component renders no `<form>` of its own (design.md D4: "fuera del `<form>`: el único disparador del `PUT` sigue siendo 'Guardar cambios' del formulario" — assert via `container.querySelector('form')` returning `null` inside the piece's own render tree).
- [x] 8.7 GREEN: create `apps/frontend/src/configuracion/piezas/CampoDominios.tsx` implementing `CampoDominiosProps { valor: string[]; onCambiar: (dominios: string[]) => void; deshabilitado?: boolean }` per design.md's exact interface — controlled, no internal fetch, no `useSesion()`, no submit.
- [x] 8.8 Full regression for PR3a (final phase, single-piece PR): run `pnpm --filter @seei/frontend test CampoDominios.spec.tsx` green, then full suite + `pnpm typecheck` clean before opening PR3a.

## Phase 9 (PR3b): D5/D6 — `PanelDatosInstitucionales`: campos de texto y precarga

- [x] 9.1 RED in `apps/frontend/src/configuracion/paneles/PanelDatosInstitucionales.spec.tsx` (new, `vi.mock('../configuracion-api')`): given a `config: ConfiguracionRespuestaDto` prop (already resolved, per D6 — the panel itself does no fetching, `ConfiguracionPage` gates mounting), renders a `FormularioGenerico` pre-filled with `nombre`, `director`, `color_primario`, `color_secundario`, `zona_horaria` from `config` (spec: `configuracion-institucional`, "Editar nombre y director sin tocar el resto").
- [x] 9.2 RED (same file): the three SMTP fields (`smtp_host`, `smtp_puerto`, `smtp_remitente`) render **empty**, not pre-filled from `config` even if a caller mistakenly passed values for them (spec: same requirement text — "los campos SMTP... son escritura pura... el formulario los renderiza vacíos"; design.md D5 — `ConfiguracionRespuestaDto` genuinely doesn't return them, this locks the UI behavior regardless), with a visible note that leaving them blank does not modify the saved value.
- [x] 9.3 RED (same file): no field in the rendered form is labeled "contraseña", "clave", or contains `password` in any form (spec: `configuracion-institucional`, "El formulario no renderiza ningún campo de contraseña SMTP" — scan the full DOM, case-insensitive).
- [x] 9.4 GREEN: create `apps/frontend/src/configuracion/paneles/PanelDatosInstitucionales.tsx` receiving `{ config, deshabilitado, onGuardado }` — no `dominios`/`onDominiosCambiar` props: confirmed at apply time (matches design.md D4's flow diagram and Phase 11.5's own instruction) that `dominios_google` state lives INSIDE this panel, seeded from `config.dominios_google`, not lifted to `ConfiguracionPage`; wire `FormularioGenerico` with the 8 string `CampoFormulario[]` (5 pre-filled + 3 empty SMTP), no password field defined anywhere in the literal array.

## Phase 10 (PR3b): D5 — diff de merge parcial y coerción de `smtp_puerto`

- [x] 10.1 RED (same file): editing only `nombre` and `director` and submitting calls `actualizarConfiguracion({ nombre, director })` — exactly those two keys, nothing else (spec scenario: "Editar nombre y director sin tocar el resto" — "se invoca `PUT /configuracion` con esos dos campos"; design.md D5's diff algorithm, `valores[k] !== iniciales[k]`).
- [x] 10.2 RED (same file): leaving all three SMTP fields blank and submitting does **not** include any `smtp_*` key in the `actualizarConfiguracion` call (design.md D5: `'' === ''` never travels — this is the diff-against-`''` mechanism that turns the backend's missing SMTP fields into correct "don't touch" semantics).
- [x] 10.3 RED (same file): typing `'8025'` into `smtp_puerto` and submitting calls `actualizarConfiguracion({ ..., smtp_puerto: 8025 })` — a `number`, not the string `'8025'` (design.md D5: `Number()` coercion, required because Prisma's `Int?` + no `ValidationPipe` means a raw string reaches Prisma and 500s).
- [x] 10.4 RED (same file): typing a non-integer value into `smtp_puerto` (e.g. `'abc'` or `'80.5'`) and submitting shows a field-level error and does **not** call `actualizarConfiguracion` at all (design.md D5: "se descarta si no es entero positivo, mostrando error de campo sin llamar al backend").
- [x] 10.5 RED (same file): on a 2xx response, the panel calls `onGuardado(dataDevuelta)` with the backend's returned `ConfiguracionRespuestaDto` (spec scenario: "Guardado exitoso refleja los valores persistidos"); on a 4xx response with `{ codigo: 'CAMPO_INVALIDO', campo: 'color_primario' }`, the panel shows `mensajeDeError(...)` in a `role="alert"` element, does **not** call `onGuardado`, and the user's edited values remain visible in the form (spec scenario: "Error 4xx del backend se muestra legible sin perder los datos ingresados").
- [x] 10.6 GREEN: implement the diff loop from design.md's "Interfaces / Contratos" `PanelDatosInstitucionales` snippet exactly (`for (const clave of CLAVES_TEXTO) if (valores[clave] !== iniciales[clave]) body[clave] = valores[clave]`), the `Number()`/integer-guard for `smtp_puerto`, and the success/failure branches (`onGuardado` vs. `mensajeDeError` + no remount, deferring to `ConfiguracionPage`'s `version`/`key` mechanism for the actual remount, D6).

## Phase 11 (PR3b): D4 — wiring de `CampoDominios` en `PanelDatosInstitucionales`

- [x] 11.1 RED (same file): the panel renders `CampoDominios` with `valor` initialized from `config.dominios_google`; adding a domain and submitting calls `actualizarConfiguracion({ dominios_google: [...nuevoArreglo] })` alongside any other changed text field (spec scenario: "Agregar un dominio válido").
- [x] 11.2 RED (same file): starting from `config.dominios_google = ['colegio.edu.pe']`, removing that entry via `CampoDominios` and submitting **with no other field changed** calls `actualizarConfiguracion({ dominios_google: [] })` — explicit empty array present in the body, not omitted (spec scenario: "Quitar el último dominio y guardar arreglo vacío explícito" — the `PUT`-time half; the panel does an array-equality diff against `config.dominios_google`, not a reference/string diff, since a fresh `[]` is never `===` the original array).
- [x] 11.3 RED (same file): submitting without touching `dominios_google` at all does **not** include that key in the body, even though `CampoDominios`'s local array reference is a new object (design.md D5's `arraysIguales` helper — reference inequality alone must not trigger a spurious diff).
- [x] 11.4 RED (same file): on a 4xx response with `{ codigo: 'CAMPO_INVALIDO', campo: 'dominios_google' }`, the previously-rendered domain list is not cleared or reset (spec scenario: "Dominio con formato inválido se rechaza antes o después del envío" — "el arreglo previamente guardado no se pierde en la UI").
- [x] 11.5 GREEN: add local `dominios` state to `PanelDatosInstitucionales` seeded from `config.dominios_google`, an `arraysIguales(a, b)` helper (order-sensitive shallow compare is sufficient — confirm at apply time whether the UI needs to tolerate reordering, design.md doesn't mandate it), and fold `dominios_google` into the diffed body per the exact snippet in design.md's "Interfaces / Contratos" section.

## Phase 12 (PR3b): D6 — cableado en `ConfiguracionPage`: carga del singleton y remount por `version`

- [x] 12.1 RED in `ConfiguracionPage.spec.tsx` (extend Phase 4's file, `vi.mock('./configuracion-api')`): with `puedeGestionar === true`, mounts and calls `obtenerConfiguracion()` once; while pending, renders a `role="status"` loading message and does **not** mount `PanelDatosInstitucionales` (design.md D6: `FormularioGenerico` initializes its state exactly once via `useState(() => …)`, so nothing may mount before the GET resolves).
- [x] 12.2 RED (same file): once `obtenerConfiguracion()` resolves `{ ok: true, data }`, `PanelDatosInstitucionales` mounts with `config={data}` (spec: `configuracion-institucional`, "Formulario de edición del singleton institucional" — "consuma `GET /configuracion` para precargar valores").
- [x] 12.3 RED (same file): after a successful save (`PanelDatosInstitucionales`'s `onGuardado` fires with the backend's returned data), `ConfiguracionPage` increments an internal `version` counter and re-renders `PanelDatosInstitucionales` with a `key={version}`, forcing a remount whose initial values are the backend's response — assert via a spy/marker that the form's rendered values now reflect the returned data, not stale local state (spec scenario: "Guardado exitoso refleja los valores persistidos"; design.md D6: `key`-forced remount is the only way to defeat `FormularioGenerico`'s one-shot `useState` initializer without touching the shared piece).
- [x] 12.4 RED (same file): after a failed save (`onGuardado` never fires, the panel shows its own alert internally per Phase 10.5), `version` does **not** change and no remount occurs — assert the form retains a value the user typed before the failed submit survives across the render (spec scenario: "Error 4xx del backend se muestra legible sin perder los datos ingresados", page-level half of D6's contract with the panel).
- [x] 12.5 GREEN: in `ConfiguracionPage.tsx`, add `useEffect` calling `obtenerConfiguracion()` on mount (only when `puedeGestionar`); track `cargando`/`config`/`version` state; render the loading status while `cargando`; mount `PanelDatosInstitucionales key={version} config={config} onGuardado={(data) => { setConfig(data); setVersion(v => v + 1); }}`.
- [x] 12.6 Full regression for PR3b (final phase, closes the PR3 split): run `pnpm --filter @seei/frontend test PanelDatosInstitucionales.spec.tsx ConfiguracionPage.spec.tsx` green, then full suite + `pnpm typecheck` clean before opening PR3b.

## Phase 13 (PR4): D8 — `validar-logo.ts` helper puro

- [x] 13.1 RED in `apps/frontend/src/configuracion/validar-logo.spec.ts` (new, no jsdom render needed — a plain `File` object suffices per design.md D11's "función pura ... sin depender del render"): a `.png` file with MIME `image/png` and `size <= 2 * 1024 * 1024` returns `null` (valid); same for `.jpg`/`.jpeg` with `image/jpeg` and `.svg` with `image/svg+xml`.
- [x] 13.2 RED (same file): a `.pdf` file returns a non-empty error string (spec: `configuracion-institucional`, "Formato no permitido se rechaza en el cliente sin llamar al backend").
- [x] 13.3 RED (same file): a file named `logo.png` but with MIME `application/pdf` (extension/MIME mismatch) returns a non-empty error string (design.md D8: "espeja `filtroArchivoLogo`" — extension paired with MIME, not either alone).
- [x] 13.4 RED (same file): a file named `logo.png.svg` is evaluated by its actual extension (`.svg`) not a naive first-match, and is accepted only if its MIME also matches `image/svg+xml` (threat matrix "Clasificación de archivo activo").
- [x] 13.5 RED (same file): a file of exactly `2 * 1024 * 1024` bytes returns `null` (valid, boundary inclusive per design.md's `<= 2MB`); a file of `2 * 1024 * 1024 + 1` bytes returns a non-empty error string (spec scenario: "Archivo que excede 2 MB se rechaza en el cliente sin llamar al backend").
- [x] 13.6 RED (same file): a file of `0` bytes returns a non-empty error string (threat matrix: "archivo de 0 bytes").
- [x] 13.7 GREEN: create `apps/frontend/src/configuracion/validar-logo.ts` exporting `validarArchivoLogo(archivo: File): string | null` implementing the extension allowlist (`.png`/`.jpg`/`.jpeg`/`.svg`), the MIME-pairs-with-extension check, the `<= 2MB` size check, and the `> 0` bytes check, per design.md D8.
- [x] 13.8 Regression checkpoint: run `pnpm --filter @seei/frontend test validar-logo.spec.ts` green before Phase 14.

## Phase 14 (PR4): D8 — `PanelLogo`

- [x] 14.1 RED in `apps/frontend/src/configuracion/paneles/PanelLogo.spec.tsx` (new, `vi.mock('../configuracion-api')`): given `{ logoPresente: true, deshabilitado }`, renders `<img src={urlLogo(...)}>` for the current logo, `CampoArchivo` for selecting a replacement, and a "Subir logo" button (design.md D8's flow diagram).
- [x] 14.2 RED (same file): given `{ logoPresente: false }`, renders **no** `<img>` element (spec/design.md D8: "vista previa ... sólo si `logo_presente`" — `GET /configuracion/logo` 404s without a logo, an unconditional `<img>` would show a broken-image icon).
- [x] 14.3 RED (same file): selecting a `.pdf` file and clicking "Subir logo" shows a `role="alert"` with a readable message and does **not** call `subirLogo` (spec scenario: "Formato no permitido se rechaza en el cliente sin llamar al backend" — "ningún request multipart se envía"); same assertion for a 3 MB file (spec scenario: "Archivo que excede 2 MB se rechaza en el cliente sin llamar al backend").
- [x] 14.4 RED (same file): selecting a valid 1 MB PNG and clicking "Subir logo" calls `subirLogo(archivo)`; on a 2xx response, the `<img>`'s `src` changes (assert the `src` attribute differs before/after, driven by `version = logo_actualizado_en` per design.md D8's cache-bust) (spec scenario: "Subir un logo válido reemplaza el existente").
- [x] 14.5 RED (same file): on a 4xx response (e.g. `{ codigo: 'LOGO_TAMANIO_EXCEDIDO' }` — server-side rejection after client validation somehow passed, or `LOGO_FORMATO_NO_PERMITIDO`), the panel shows `mensajeDeError(...)` in a `role="alert"` element.
- [x] 14.6 RED (same file): `deshabilitado === true` disables the upload control entirely (D10 propagation — this panel is "ciego al rol" per design.md, the gate lives only in `ConfiguracionPage`, but it still receives and respects a boolean prop for the loading-gate case, D6). Implemented: `CampoArchivo` stays unmodified (D8), so `deshabilitado` gates the "Subir logo" button only — same precedent as `FormularioLista`'s submit-button gating; no `disabled` prop exists on `CampoArchivo` to touch.
- [x] 14.7 GREEN: create `apps/frontend/src/configuracion/paneles/PanelLogo.tsx` — `CampoArchivo` with `aceptar="image/png,image/jpeg,image/svg+xml"` (unmodified piece, per design.md D8), `validarArchivoLogo` gate before any call to `subirLogo`, `<img src={urlLogo(version)}>` conditional on `logoPresente`, `version` state updated to `logo_actualizado_en` on success.

## Phase 15 (PR4): D9 — `PanelComite`

- [x] 15.1 RED in `apps/frontend/src/configuracion/paneles/PanelComite.spec.tsx` (new): given `{ integrantes: [...] }` with rows, renders them via `TablaGenerica` with columns `nombres`/`dni`/`codigo`/`correo`/`estado` (spec: `configuracion-institucional`, "Lista de comité solo lectura, sin acciones de edición").
- [x] 15.2 RED (same file): no "Crear", "Editar", "Cambiar estado", or "Eliminar" button/action exists anywhere in the rendered panel — assert via `screen.queryByText(/crear|editar|cambiar estado|eliminar/i)` returning `null`, scoped to this panel's container (spec scenario: "La lista de comité se renderiza sin controles de escritura").
- [x] 15.3 RED (same file): given `{ integrantes: [] }`, renders a readable empty-state message (`mensajeVacio`), not an error, not a blank panel (spec scenario: "Lista de comité vacía no rompe la vista").
- [x] 15.4 GREEN: create `apps/frontend/src/configuracion/paneles/PanelComite.tsx` rendering `TablaGenerica` **without** passing the `acciones` prop at all (design.md D9: omitting the prop, not `acciones={[]}`, so intent is verifiable by reading the module — no `AccionFila` declared anywhere) and `mensajeVacio="No hay integrantes del comité registrados."`.
- [x] 15.5 Regression checkpoint: run `pnpm --filter @seei/frontend test PanelLogo.spec.tsx PanelComite.spec.tsx` green before Phase 16.

## Phase 16 (PR4): Cableado final en `ConfiguracionPage`

- [x] 16.1 RED in `ConfiguracionPage.spec.tsx`: with `puedeGestionar === true`, mounts and calls `listarComite()` in addition to `obtenerConfiguracion()` (both fire once, order-independent — assert via mock call counts, not sequencing) (design.md flow diagram: `obtenerConfiguracion() ‖ listarComite()`, parallel, not chained).
- [x] 16.2 RED (same file): once both resolve, `PanelLogo` mounts with `logoPresente`/`logoMime` derived from the `obtenerConfiguracion()` response, and `PanelComite` mounts with `integrantes` from the `listarComite()` response — both alongside `PanelDatosInstitucionales` from Phase 12, none of the three gated on each other's fetch (design.md D6: only the singleton `GET` gates the form; comité's own list has its own independent empty-state handling from Phase 15.3, it doesn't need to block the page).
- [x] 16.3 RED (same file): a failed `listarComite()` (non-2xx) does **not** prevent `PanelDatosInstitucionales`/`PanelLogo` from rendering — assert the page as a whole degrades gracefully per-section rather than failing wholesale (reasonable inference from D6's "no monta hasta que resuelve el GET" applying specifically to the singleton form, not to comité).
- [x] 16.4 GREEN: in `ConfiguracionPage.tsx`, add the `listarComite()` fetch alongside `obtenerConfiguracion()`, mount `PanelLogo` and `PanelComite` with their respective props once each has resolved (independently of each other and of the singleton's `version`/remount cycle).
- [x] 16.5 Full regression for PR4 (final PR of the chain): run `pnpm --filter @seei/frontend test validar-logo.spec.ts PanelLogo.spec.tsx PanelComite.spec.tsx ConfiguracionPage.spec.tsx` green, then the FULL `pnpm --filter @seei/frontend test` suite and `pnpm typecheck` clean before opening PR4.

## Cross-cutting checklist (verify once, applies across all 5 PRs)

- [x] Confirm `puedeGestionar` in `ConfiguracionPage` is derived **once**, as a single allowlist expression (`rol === 'administrador' || rol === 'director'`), never as a denylist — design.md D10 explicitly names this as the criterion, same discipline as `#27` D4 but with a single gate instead of two, since all three sections share one role set. Verified: single `const puedeGestionar = rol === 'administrador' || rol === 'director'` in `ConfiguracionPage.tsx`, unchanged since PR1.
- [x] Confirm `PanelDatosInstitucionales`, `PanelLogo`, and `PanelComite` are all "blind to role" — none of them call `useSesion()` internally; each receives only the plain props (`deshabilitado`, data, callbacks) it needs from `ConfiguracionPage` (design.md flow diagram: "Los tres paneles son ciegos al rol"). Verified via grep: the only `useSesion` occurrences in `apps/frontend/src/configuracion` are doc-comment mentions of its absence (`PanelLogo.tsx`, `PanelComite.tsx`, `CampoDominios.tsx`, plus the real call in `ConfiguracionPage.tsx` itself); zero panels/piezas import or call it.
- [x] For every write wrapper in `configuracion-api.ts` that can fail with a `codigo`: confirm the consuming panel passes `{ codigo, campo, status }` through to `mensajeDeError` unchanged, rather than re-deriving its own message string (same discipline as `#26`/`#27`). Verified: `PanelDatosInstitucionales` and `PanelLogo` both call `mensajeDeError({ codigo: resultado.codigo, campo: resultado.campo, status: resultado.status })` unchanged.
- [x] Confirm no `dangerouslySetInnerHTML` exists anywhere in `apps/frontend/src/configuracion` — the logo is always rendered via `<img src>`, never injected inline, per the threat matrix's "Clasificación de archivo activo" mitigation. Verified via grep: the only occurrence is a doc-comment in `PanelLogo.tsx` explicitly stating it is prohibited/unused; zero JSX/API usage. `PanelLogo` renders the logo exclusively via `<img src={urlLogo(version)}>`.
- [x] Confirm `CampoDominios.tsx` never imports `configuracion-api.ts` and has no `useEffect`/fetch of its own — verify via grep that the piece stays presentational per D4. Verified via grep: no `useEffect`, `fetch(`, or `configuracion-api` import in `CampoDominios.tsx` (unchanged since PR3a).
- [x] Grep `apps/frontend/src/configuracion` for any `className` token not already in the project's approved token list (same audit as `#25`/`#26`/`#27`); confirm nothing new was added to `tailwind.config.*`/`index.css`. Verified: all tokens used in `PanelLogo.tsx`/`PanelComite.tsx` (`rounded-control`, `border-border-gray`, `text-title-md`, `text-body-sm`, `text-error`, `bg-primary`, `text-on-primary`, `text-on-surface`, `text-label-md`, etc.) are pre-existing tokens already used elsewhere (`academico/`, `procesos/`); no edits to `tailwind.config.*`/`index.css` in this change.
- [x] Confirm no file in `apps/frontend/src/configuracion` imports from `apps/frontend/src/usuarios` or `apps/frontend/src/academico` — the comité list is served entirely by `configuracion-api.ts`'s own `listarComite`, not `usuarios-api.ts`, per design.md D3's explicit rejection of "colgar el comité de `usuarios-api.ts`". Verified via grep: zero matches for imports from `../../usuarios` or `../../academico` anywhere in `apps/frontend/src/configuracion`.

## Post-chain: full suite sanity (run after PR4 merges)

- [x] Run `pnpm --filter @seei/frontend test` (entire suite, all 5 PRs' specs together) and confirm green. Result: 78 test files, 550 tests, all passed.
- [x] Run `pnpm typecheck` and confirm clean (validates the `Ruta`/`ItemMenu` union exhaustiveness and the `Record<CodigoConfiguracion, string>` totality end-to-end). Result: all 4 packages (`@seei/backend`, `@seei/contracts`, `@seei/frontend`, `@seei/worker`) typecheck clean.
- [x] Confirm the proposal's Success Criteria checklist: `administrador`/`director` edit `nombre`/`director`/colores/zona horaria/dominios Google/SMTP (sin contraseña) from `/configuracion` without calling the API by hand; the institutional logo uploads/replaces from the same page via `CampoArchivo`; the comité list renders read-only with zero write actions; `comite` doesn't see the `configuracion` menu item. Verified: all wired end-to-end in `ConfiguracionPage.tsx` (`PanelDatosInstitucionales` + `PanelLogo` + `PanelComite`), covered by the corresponding spec files across PR1-PR4; `menu-por-rol.spec.ts` (PR1) asserts `comite` doesn't see the item.
- [x] Before archiving, re-verify `openspec/changes/frontend-configuracion-general/specs/configuracion-institucional/spec.md`'s SMTP wording still matches what got built (the delta was already corrected pre-`sdd-tasks` to state SMTP fields are pure write with no precarga — confirm no stale "precarga" wording slipped back in during apply, same discipline `#27`'s Post-chain checklist applied to its own delta). Verified: the delta spec's SMTP scenario says the fields "son de escritura pura (no se precargan)" — matches the built behavior (`PanelDatosInstitucionales` renders `smtp_*` always empty, D5). No stale "precarga" wording found.
- [x] Log the two backlog candidates design.md's "Preguntas abiertas" raised for `#7` (exposing `smtp_*` in `ConfiguracionRespuestaDto` + an explicit "Limpiar" action; adding `ValidationPipe`/`class-validator` to `ActualizarConfiguracionDto` so `smtp_puerto` 400s instead of 500s) as follow-up backlog items — out of scope for this change, not silently dropped. Logged below in "Follow-up backlog candidates (out of scope for #28)".

### Follow-up backlog candidates (out of scope for #28, logged for `#7`)

1. Expose `smtp_host`/`smtp_puerto`/`smtp_remitente` in `ConfiguracionRespuestaDto` and add an explicit "Limpiar" action in the UI, so a saved SMTP value can be cleared/edited without the diff-against-`''` limitation (design.md "Preguntas abiertas").
2. Add `class-validator`/`ValidationPipe` to `ActualizarConfiguracionDto` so an invalid `smtp_puerto` (or other malformed field) returns a `4xx` from the backend instead of a raw Prisma `500` — the client-side coercion in `PanelDatosInstitucionales` mitigates this but the real fix is server-side.
